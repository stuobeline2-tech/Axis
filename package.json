{
  "name": "axis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Presell machine for unsold-estimate recovery sold to home service contractors",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "leads": "node --no-warnings lead-engine/cli.mjs",
    "pipeline": "node --no-warnings pipeline/cli.mjs",
    "funnel": "node --no-warnings pipeline/cli.mjs funnel",
    "next": "node --no-warnings pipeline/cli.mjs next",
    "test": "node --no-warnings --test test/*.test.mjs"
  }
}
